import { StyleSheet } from 'react-native';
import { colors, fontFamily2, screenContainer } from '../../shared/styles/styles';

const SearchFiltersStyles = StyleSheet.create({
  screenContainer,
});

export default SearchFiltersStyles;
