import { StyleSheet } from 'react-native';
import { colors, fontFamily2 } from '../../styles/styles';

const SSubmitButtonStyles = StyleSheet.create({
  submitButton: {marginTop: 16, borderWidth: 1, borderRadius: 8, borderColor: colors.lightGray, alignItems: 'center', padding: 6},
});

export default SSubmitButtonStyles;
